# Screenshots

The root `README.md` references two images. Drop them in here before the
public push (filenames must match exactly):

- **`library.png`** — the Library view: the document list with the coloured
  per‑target status badges, search bar, and group headers.
- **`editor.png`** — a document open in the editor showing a figure (original
  photo beside its NotebookLM‑safe sketch), with the right‑hand Images /
  Pending‑changes / Snapshots rail and the left‑hand outline visible.

PNG is preferred. Once both files are added, delete this `README.md`.

> Note: these views contain real memoir content. Use a document you're happy
> to show publicly, or redact as needed before pushing.
